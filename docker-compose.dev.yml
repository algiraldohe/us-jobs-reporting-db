version: '3.8'
services:
  db:
    image: postgres:14.1-alpine
    restart: always
    env_file:
      - transformation-load/.env
    volumes:
      - ./transformation-load/postgres-data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
